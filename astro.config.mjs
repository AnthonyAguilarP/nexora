// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Site URL used by Astro for absolute URLs (sitemap, canonical, etc.)
	site: 'https://www.nexoraorg.com',
});
